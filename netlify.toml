[build]
  command = "ng build --configuration production && node replace.js"
  publish = "dist"

[context.production.environment]
  ANGULAR_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200